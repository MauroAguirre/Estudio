window.onload = Load;function Load(){	var btn = document.getElementsByClassName("btnBorrar");	for(var i=0;i<btn.length;i++)		btn[i].onclick = Borrar;}function Borrar(){	var pepe = this;	// obtener el objeto que hice la accion	var colum = pepe.parentNode;	// ir al nodo padre	var fila = colum.parentNode;	var table = fila.parentNode;	var atrFila = fila.getAttribute("filaId");//	var atrFila = this.parentNode.parentNode.getAttribute("filaId");//	fila.innerHTML=""; con esto borramos la tabla pero quedan los td//	fila.remove();  con esto borramos la table mejor//	alert(atrFila);  mostramos el atributo	var filas = table.childNodes;	var fila1;	for(var l=0;l<filas.length;l++){		var atr = filas[l].getAttribute("filaId");		if(atr= = "f1")			fila1=filas[l];	}	var hermanos = fila1.	// para obtener hermanos}$(document).ready(function(){	});